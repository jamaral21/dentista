import { useState } from "react";
import { contactItems } from "@/content/site-content";
import type { DemoAction } from "@/types/site";

export function Contacto({ onDemoAction }: { onDemoAction: (action: DemoAction) => void }) {
  const [sent, setSent] = useState(false);
  const [form, setForm] = useState({ nombre: "", telefono: "", mensaje: "" });
  const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); setSent(true); setForm({ nombre: "", telefono: "", mensaje: "" }); };
  return <section id="contacto" className="py-20 md:py-28" style={{ background: "#F5F8FA" }} aria-label="Contacto"><div className="max-w-6xl mx-auto px-5"><div className="text-center max-w-xl mx-auto mb-14"><span className="inline-block text-xs font-semibold tracking-widest uppercase mb-4 px-3 py-1.5 rounded-full" style={{ color: "#55D6A9", background: "rgba(85,214,169,0.1)", border: "1px solid rgba(85,214,169,0.2)", fontFamily: "Sora, sans-serif" }}>Contacto</span><h2 className="text-3xl md:text-4xl font-bold mb-4" style={{ color: "#081D2C", fontFamily: "Sora, sans-serif", letterSpacing: "-0.02em" }}>Da el primer paso para cuidar tu sonrisa</h2><p className="text-base" style={{ color: "rgba(8,29,44,0.62)", fontFamily: "Inter, sans-serif" }}>Estamos para resolver tus dudas y ayudarte a encontrar el mejor momento para tu consulta.</p></div><div className="grid md:grid-cols-2 gap-10 items-start"><div className="flex flex-col gap-6"><div className="flex flex-col gap-4"><a href="#contacto" onClick={(event) => { event.preventDefault(); onDemoAction("whatsapp"); }} className="btn-primary px-6 py-4 text-sm text-center">Solicitar cita por WhatsApp</a><button type="button" onClick={() => onDemoAction("phone")} className="btn-outline-dark px-6 py-4 text-sm w-full">Llamar al consultorio</button></div><div className="rounded-2xl p-6" style={{ background: "#fff", border: "1px solid rgba(8,29,44,0.08)", boxShadow: "0 2px 12px rgba(8,29,44,0.06)" }}><h3 className="text-sm font-semibold mb-4" style={{ color: "#081D2C", fontFamily: "Sora, sans-serif" }}>Datos de contacto</h3><dl className="flex flex-col gap-3">{contactItems.map((item) => <div key={item.label} className="flex items-start gap-3"><span className="text-base mt-0.5 flex-shrink-0" aria-hidden="true">{item.icon}</span><div><dt className="text-xs font-medium" style={{ color: "rgba(8,29,44,0.45)", fontFamily: "Inter, sans-serif" }}>{item.label}</dt>{item.action ? <button type="button" className="text-sm text-left underline underline-offset-2" style={{ color: "#081D2C", fontFamily: "Inter, sans-serif" }} onClick={() => onDemoAction(item.action!)}>{item.value}</button> : <dd className="text-sm" style={{ color: "#081D2C", fontFamily: "Inter, sans-serif" }}>{item.value}</dd>}</div></div>)}</dl></div><div className="rounded-2xl overflow-hidden relative" style={{ height: 180, background: "#E8F0F5", border: "1px solid rgba(8,29,44,0.08)" }} role="img" aria-label="Tarjeta de ubicación de demostración — no representa un mapa real"><div className="absolute inset-0 flex flex-col items-center justify-center gap-2" style={{ background: "linear-gradient(135deg, #E8F4FB 0%, #EEF7F2 100%)" }}><div className="w-10 h-10 rounded-full flex items-center justify-center" style={{ background: "#081D2C" }} aria-hidden="true">⌖</div><p className="text-xs font-semibold" style={{ color: "#081D2C", fontFamily: "Sora, sans-serif" }}>Av. Ejemplo 120, Querétaro</p><p className="text-xs" style={{ color: "rgba(8,29,44,0.5)", fontFamily: "Inter, sans-serif" }}>Ubicación de demostración</p></div></div></div><div className="rounded-2xl p-7" style={{ background: "#fff", border: "1px solid rgba(8,29,44,0.08)", boxShadow: "0 4px 20px rgba(8,29,44,0.08)" }}><h3 className="text-lg font-semibold mb-1" style={{ color: "#081D2C", fontFamily: "Sora, sans-serif" }}>Escríbenos tu consulta</h3><p className="text-xs mb-6 px-3 py-2 rounded-lg" style={{ color: "rgba(8,29,44,0.5)", background: "rgba(8,29,44,0.03)", fontFamily: "Inter, sans-serif", border: "1px solid rgba(8,29,44,0.06)" }}>⚠️ Formulario simulado — no recopila ni transmite datos reales. Solo para demostración.</p>{sent ? <div className="flex flex-col items-center justify-center gap-4 py-12 text-center" role="status" aria-live="polite"><span className="w-14 h-14 rounded-full flex items-center justify-center text-2xl" style={{ background: "rgba(85,214,169,0.12)", border: "2px solid rgba(85,214,169,0.3)" }} aria-hidden="true">✓</span><div><p className="font-semibold text-base" style={{ color: "#081D2C", fontFamily: "Sora, sans-serif" }}>¡Mensaje simulado enviado!</p><p className="text-sm mt-1" style={{ color: "rgba(8,29,44,0.55)", fontFamily: "Inter, sans-serif" }}>No se transmitieron datos. En un escenario real, aquí se confirmaría el siguiente paso.</p></div><button type="button" onClick={() => setSent(false)} className="btn-outline-dark px-5 py-2.5 text-sm">Enviar otro mensaje</button></div> : <form onSubmit={submit}><div className="flex flex-col gap-4">{(["nombre", "telefono", "mensaje"] as const).map((field) => <div key={field}><label htmlFor={field} className="block text-xs font-medium mb-1.5" style={{ color: "rgba(8,29,44,0.65)", fontFamily: "Inter, sans-serif" }}>{field === "nombre" ? "Nombre completo" : field === "telefono" ? "Teléfono" : "Mensaje o consulta"}</label>{field === "mensaje" ? <textarea id={field} required rows={4} value={form[field]} onChange={(event) => setForm({ ...form, [field]: event.target.value })} placeholder="¿En qué podemos ayudarte?" className="w-full px-4 py-3 text-sm rounded-xl resize-none" /> : <input id={field} type={field === "telefono" ? "tel" : "text"} required={field === "nombre"} autoComplete={field === "nombre" ? "name" : "tel"} value={form[field]} onChange={(event) => setForm({ ...form, [field]: event.target.value })} placeholder={field === "nombre" ? "Tu nombre" : "442 000 0000"} className="w-full px-4 py-3 text-sm rounded-xl" />}</div>)}<button type="submit" className="btn-primary px-6 py-3.5 text-sm w-full">Enviar mensaje</button></div></form>}</div></div></div></section>;
}
